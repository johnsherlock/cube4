import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.johnsherlock.cube4",
  appName: "CUBE4",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
